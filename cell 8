# Cell 8

# Determine the total number of steps based on the number of images and batch size
total_train = 2000
total_val = 1000
epochs = 15 # You may need to tweak this (15-25 is a good range)

history = model.fit(
    x=train_data_gen, # Training data generator
    steps_per_epoch=total_train // BATCH_SIZE, # Number of steps for one epoch
    epochs=epochs,
    validation_data=val_data_gen, # Validation data generator
    validation_steps=total_val // BATCH_SIZE # Number of steps for validation
)
